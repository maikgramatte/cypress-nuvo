{
  "name": "cypress",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "cypress": "3.8.0"
  },
  "scripts": {
    "test": "cypress open",
    "local-test": "cypress open --config-file cypress.local.json"
  }
}
